// Diferencia entre metodo y funcion
const numero1 = 20;
const numero2 = "20";

console.log(parseInt(numero2));  // parseInt() es una función que convierte un string a un numero entero
console.log(numero1.toString());  // toString() es un método ya que se le aplica a una variable seguida de un punto 
